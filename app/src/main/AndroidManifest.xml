<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permissions required for Maps + Location + Places API -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:name=".RoamioApp"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Roamio">

        <activity
            android:name=".activities.ProfileActivity"
            android:theme="@style/Theme.Roamio.Signup"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <activity
            android:name=".activities.TripActivity"
            android:theme="@style/Theme.Roamio.Signup"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <activity android:name=".activities.MyTripsActivity"
            android:theme="@style/Theme.Roamio.Signup"
            android:exported="false"/>

        <!-- MAIN ACTIVITY -->
        <!--
            ✅ Google Maps API Key
            Value is injected from local.properties via build.gradle.kts.
            Never hard-code your key here — keep it in local.properties.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}"/>

        <!-- MAIN / HOME ACTIVITY — session check inside redirects to Login if needed -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Roamio.Home"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- LOGIN ACTIVITY -->
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/Theme.Roamio.Signup"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <!-- SIGNUP ACTIVITY -->
        <activity
            android:name=".activities.SignupActivity"
            android:theme="@style/Theme.Roamio.Signup"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <!-- ✅ NEARBY ACTIVITY -->
        <activity
            android:name=".activities.NearbyActivity"
            android:theme="@style/Theme.Roamio.Home"
            android:exported="false" />

    </application>

</manifest>
